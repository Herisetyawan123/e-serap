import "./grid-item.scss"

interface Props{
    img: string,
    label: string,
    to?: string
}

const GridItem = ({ img, label, to }:Props ) => {
    return (
        <></>
    )
}

export  default GridItem;